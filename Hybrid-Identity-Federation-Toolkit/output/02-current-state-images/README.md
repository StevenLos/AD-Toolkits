# Current-State Images

Rendering writes current-state hybrid identity topology diagrams here:

- `current-state-combined.svg`
- `current-state-combined.png`
- `current-state-federation.svg`
- `current-state-federation.png`
- `current-state.topology-relationships.csv`

PNG files are skipped when `-SkipPng` is used or when a supported browser is unavailable.
