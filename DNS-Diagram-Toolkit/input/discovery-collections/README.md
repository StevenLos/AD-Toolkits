# Discovery Collections

Place `*.collection.json` and `*.sites.collection.json` files here before running aggregation.

DNS discovery writes `*.collection.json` files. Optional `*.sites.collection.json` files should come from the sibling `AD-Sites-Services-Topology-Toolkit` folder.
