# Examples

This folder will contain safe sample inventories, CSVs, and rendered DNS diagram outputs.

Start with:

```text
sample-dns-relationship-details.csv
```

It can be rendered without live DNS or AD access.
