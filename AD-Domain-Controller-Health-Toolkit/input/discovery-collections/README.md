# Discovery Collections

Place `*.dc-health.collection.json` files from `01-discovery` here before running `02-aggregation`.

Optional sibling time inventory is read from `../Time-Server-Toolkit/output/01-merged-inventory/inventory.json` by default, or can be passed to the merger with `-TimeInventoryJson`.
